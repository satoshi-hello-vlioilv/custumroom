# custumroom — ルームプランナー 3D

ブラウザで動く 3D ルームプランニングアプリです。ビルド工程は不要で、依存は CDN から読み込みます。コードは責務ごとに ES モジュールへ分割されており、モデルの追加・編集がしやすい構成になっています。

## デモ

ES モジュール（`js/` 配下）を使用しているため、`index.html` を `file://` で直接開くとブラウザの CORS 制限で読み込みに失敗します。**ローカルサーバー経由で開いてください。**

```bash
python3 -m http.server 8080
# → http://localhost:8080/ をブラウザで開く
```

## 主な機能

### 3D ビュー
- **Three.js r160** による PBR レンダリング（物理ベースのマテリアル）
- スマートカットアウェイモード：外壁を自動で透過表示
- 軌道カメラ操作（ドラッグ・ピンチで回転・ズーム）

### 2D フロアプランエディタ
- グリッドスナップ（0.5m 単位）で壁と床セルが完全一致
- 壁の描画・削除、床の塗りつぶし
- 開口部（ドア・窓・ガラス戸）の配置
- 3D ビューとリアルタイム同期

### 壁タイプ
| タイプ | 説明 |
|--------|------|
| 普通の壁 | 不透明な壁 |
| 窓付き壁 | 腰高窓（サッシ＋ガラスペイン）付き |
| ガラス壁 | 全面透明ガラス（アルミフレーム付き）。開口（ガラスドア／出入口）にも対応し、間仕切りをガラス化できる（ABWオフィスのプロジェクトルーム・集中ブースに採用）。**壁を「不透過」表示にしてもガラス壁・窓ガラスは素材がガラスのため常に透過（最低30%）を保つ** |

### 開口部タイプ（扉ごとに「使えるシーン」設定を保持）
各扉種類は適したシーン（用途カテゴリ）情報を持ち、2Dエディタの「開口タイプ」選択に併記されます。

| タイプ | 説明 | 主なシーン |
|--------|------|-----------|
| ドア（片開き） | 標準的なスイングドア | 住宅・オフィス・店舗・工場・特殊 |
| 両開きドア | 中央で開く2枚扉 | 店舗・オフィス・特殊 |
| ガラス引戸 | 全高ガラスパネルの引き戸 | オフィス・店舗・工場・特殊 |
| 自動ドア | 左右に開くガラス自動ドア | 店舗・オフィス・特殊 |
| 浴室ドア | 防水パッキン付きの浴室扉 | 住宅 |
| トイレドア | 小幅のトイレ扉 | 住宅 |
| 実験室ドア | 観察窓付きスチール扉 | 特殊 |
| 襖（ふすま） | 和室の引き違い襖 | 住宅 |
| 障子 | 和紙の引き違い障子 | 住宅 |
| 窓 | 腰高窓（框＋ガラスペイン） | 全シーン |

### 床材
| 床材 | 説明 |
|------|------|
| 木目フローリング | |
| コンクリート | |
| タイル | |
| 大理石 | |
| カーペット | |
| 畳 | |
| 玄関タイル | |
| 土 | 屋外用 |
| 草 | 屋外用 |
| 芝生 | 屋外用 |

### 壁紙
クリーム / ホワイト / ネイビー / セージグリーン / 腰壁パネル / レンガ / 打ちっぱなし / 木目パネル

### 家具
カテゴリ別に 40 種類以上の 3D 家具モデルを用意。

| カテゴリ | アイテム例 |
|----------|-----------|
| 座る | 3 人掛けソファ、1 人掛けソファ、ダイニングチェア、オフィスチェア |
| テーブル | ダイニングテーブル、コーヒーテーブル、デスク、サイドテーブル |
| 寝室 | シングルベッド、ダブルベッド |
| 収納 | ワードローブ、本棚、チェスト、テレビボード、下駄箱、クローゼット |
| キッチン | システムキッチン、食器棚、ガスコンロ、冷蔵庫、電子レンジ、炊飯器 |
| 水回り | トイレ、手洗い器、洗面台、洗濯機、浴槽、ユニットバス |
| インテリア | フロアランプ、観葉植物、テレビ、壁掛けテレビ、ラグ |
| オフィス／店舗 | 受付カウンター、会議テーブル、ホワイトボード、**連続デスク（隙間なく並ぶベンチデスク）**、**ゴンドラ什器（両面・端を突き合わせる売場棚）**、レジ、ショーケース冷蔵庫 |
| 工場 | 作業台、ツールキャビネット、コンベアベルト、産業ロボット、CNC 工作機械、パレットラック、制御盤 |

> **連続配置アイテム**: 「連続デスク」「ゴンドラ什器」は天板・棚が footprint 全幅を占め、横（連続デスク）や端（ゴンドラ）に並べると隙間なく一続きになります。オフィスのベンチデスク島やコンビニの陳列棚列に最適です。

### プリセット
- 家族向け住宅（リビング・キッチン・寝室・バスルーム付き）
- 工場レイアウト
- その他プリセット

### その他
- 家具の色変更・回転・削除
- 家具の積み重ね（植物、テレビなど）
- アンドゥ / リドゥ（Ctrl+Z / Ctrl+Y）
- レイアウトの保存 / 読み込み（JSON 形式、v5 フォーマット）
- ステータスバーによる操作ガイド

## 技術スタック

| 項目 | 内容 |
|------|------|
| レンダリング | Three.js r160 |
| ビルドツール | 不要（ES モジュール、CDN 読み込み） |
| フォント | Inter / Noto Sans JP（Google Fonts） |
| アイコン | Font Awesome 6.5 |
| 依存ライブラリ | es-module-shims 1.8.3 |

## 使い方

### 基本操作

| 操作 | 説明 |
|------|------|
| 左ドラッグ | カメラ回転 |
| 右ドラッグ / 中ドラッグ | カメラパン |
| スクロール | ズーム |
| 家具カタログにマウスオーバー | 3Dプレビュー（小窓でモデルを回転表示し、メニュー内で選びやすく） |
| 家具クリック | 選択（プロパティパネルが開く） |

### 壁モード

ツールバーの「壁モード」をオンにして左ドラッグすると壁を描画できます。  
2D エディタ（「2D 編集」ボタン）では壁タイプ・開口部タイプを選択して詳細編集が可能です。

### 床ペイントモード

「床ペイント」をオンにして床材を選択後、左ドラッグで床を塗り、右ドラッグで消せます。

### 保存・読み込み

- 「保存」ボタン：JSON ファイルをダウンロード
- 「読み込み」ボタン：保存した JSON を読み込む

## 開発

```bash
# リポジトリをクローン
git clone https://github.com/satoshi-hello-vlioilv/custumroom.git
cd custumroom

# ローカルサーバーを起動して開く（ES モジュールのため file:// 不可）
python3 -m http.server 8080
# → http://localhost:8080/
```

## ファイル構成

責務ごとにファイルを分割しています。新しい家具・機械モデルを追加するときは、
**該当する `js/builders/*.js` に `buildXxx()` を追加 → `js/catalog.js` の
`FURNITURE_DEFS` に 1 行登録** するだけで済みます（必要なら `js/builders/<file>.js`
の `export { ... }` にも名前を追加）。

```
custumroom/
├── index.html              # HTML マークアップのみ（importmap + js/app.js を読み込む）
├── css/
│   └── style.css           # 全スタイル
└── js/
    ├── app.js              # アプリ状態・部屋/壁・配置・選択・UI・2D エディタ・保存・起動
    ├── catalog.js          # FURNITURE_DEFS（家具カタログ定義。各 builder を参照）
    ├── presets.js          # プリセット（部屋レイアウト）定義
    ├── core/
    │   ├── scene.js        # renderer / scene / camera / ライト / 環境マップ
    │   ├── textures.js     # 手続き的テクスチャ生成・床/壁タイプ定義
    │   ├── helpers.js      # 幾何ヘルパー（mat/box/cyl/cylAt 等）・定数・COLORS
    │   └── util.js         # 純粋ユーティリティ（clamp / shade）
    └── builders/           # ★モデル生成関数（責務＝用途ごとに分割）
        ├── furniture.js    # 住宅家具・照明・装飾
        ├── household.js    # キッチン・水回り・家電・収納
        ├── office.js       # オフィス・店舗・展示什器
        ├── plants.js       # 観葉植物シリーズ
        ├── industrial.js   # 大型工場機械・搬送・物流（CNC/ロボット/プレス 等）
        ├── lab.js          # 実験室・工房・試験/工作機器
        └── instruments.js  # 分析機器（HPLC・分光光度計 等）
```

### モジュール依存関係

```
util ← textures ← helpers ← builders/* ← catalog ← presets ← app
scene ← textures              scene ← app
```

すべて純粋な ES モジュールで、ビルドツールは不要です。

## ライセンス

MIT
